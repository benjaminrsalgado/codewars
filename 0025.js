function makeUpperCase(str) {
    const makeUpperCase = str => str.toUpperCase();
}

/* 
1.There is a function call makeUpperCase with a parameter (str) espera que el usuario ponga una cadena de palabras

2.se declara una constante también llamada makeUpperCase. Esta constante tiene asignada una función de flecha (str => str.toUpperCase()). Esta función de flecha toma un parámetro str y aplica toUpperCase() a la cadena para convertirla en mayúsculas. Sin embargo, esta función de flecha se declara y se define pero nunca se utiliza.