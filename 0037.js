// We need a function that can transform a number (integer) into a string.

function numberToString(num) {
    let cadena = num.toString()
    return cadena
}

// let cadena es una variable que es creada para almacenar el valor de num convertido a string

// num.toString() es una función que convierte el valor de num a string

// return cadena es una función que devuelve el valor de cadena