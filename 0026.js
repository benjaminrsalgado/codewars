function reverseList(list) {
    list.reverse()
    return (list)
    }

/* 
1. se utilliza el metodo reverse, antes de poner el metodo se debe de poner a cual objeto se le va a aplicar en este caso seria list 
2. return con el objeto que se le aplico anteriormente, ahora todoo lo que ponga el usuario se vera al reves
3. make this an excersice, continue with this
*/